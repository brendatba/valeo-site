# Serverless form endpoint that receives orders (Formspree free tier: https://formspree.io → new form → copy the endpoint URL).
# Leave empty and the site still works: every order renders on screen with SMS + email hand-off links.
VITE_ORDER_ENDPOINT=
