# BLOCKERS.md — nothing hard-blocked; defaults applied and flagged

| # | Question | Default applied | Where it shows | Needs |
|---|---|---|---|---|
| 1 | Shauna's Venmo handle is not in the brief | `business.venmo` left empty; confirmation page says "Shauna will text you her Venmo handle". A Venmo deep link (amount + memo prefilled) switches on automatically once the handle is filled in. | `data/catalog.json` → `business.venmo` | Shauna |
| 2 | Order endpoint key | `VITE_ORDER_ENDPOINT` unset; site works with SMS + mailto hand-off. Formspree form still to be created and the key added as a repo secret. | `.env.example`, `.github/workflows/deploy.yml` | Brenda (5 min) |
| 3 | Option C price | $57.50 by the 15% rule, per brief §12 | catalog `bundles.flights[C]` computes it; not hardcoded | Shauna to confirm |
| 4 | Delivery radius / fee | Free; "Shauna will confirm your delivery window by text" | `business.deliveryNote` | Shauna |
| 5 | 4 minis = $20 = 5 minis | Brief pricing means the 4th and 5th mini cost the same. Kept exactly as specified; the builder nudges "add 1 more for the $20 set". | `pricing.ts` sampleTotal | Shauna may want 4 minis = $18 or similar |
| 6 | Pillars "No Chemicals" / "Organic" | Brief §4 says verbatim, so they are on the site. The earlier SITE-ENGINE `BRIEF.md` in `~/Work/SHAUNA/` ruled those two claims dropped. Newer brief wins; flagging the conflict. | `business.pillars` | Brenda's ruling |
| 7 | Salt Scrub has no photo of its own | Reuses the sugar-scrub render | `public/photos.json` | Real photos |
| 8 | Coffee scent flat-lay is a dark-brown image | Kept (it is the only coffee asset); reads darker than the Pinterest direction | `catalog.fragrances[coffee].hero` | Photo swap |
| 9 | Live site replaced | Pages switched from "deploy from main" (legacy) to GitHub Actions, and `v2` was added to the `github-pages` environment's allowed deployment branches (the first run was rejected by that rule). Pushes to `v2` deploy while the PR is open. After merge: set the workflow's branch list back to `main` only and remove the `v2` branch policy from the environment (repo Settings → Environments → github-pages). | `.github/workflows/deploy.yml` | Brenda at merge time |
| 10 | 13 scents had no imagery; coffee's world was dark | RESOLVED 2026-09-11 late: 13 worlds + coffee-on-cream generated with Gemini (~$1 on the Work/.env key), 23 Kling loops on Kie after Brenda said "kie is available" (~1.9k credits). Dark coffee master kept at `/tmp/valeo-gen/world-coffee-dark.jpg` only. | `public/products/`, `public/video/` | — |
| 11 | AI jar render label text is legible at hero scale | Kept: it is the only jar asset. Flagged as the first material finding in `REVIEW.md`. | `public/products/jar-body-butter.webp` | Shauna's shoot (shot list issued) |
| 12 | Stale video trees in git on `main` (`assets/light/*.mp4`, `assets/w-*.mp4`, `assets/w-*-m.mp4`, the last mislabeled as mobile) | v2 branch deleted them from the tree; history keeps them. | git history | optional cleanup |
| 13 | **Orders reach Shauna only if the customer sends them** (shopper-QA blunt finding, round 4) | The confirmation now makes "Text it to Shauna" Step 1 (the SMS composer opens automatically on phones with the whole order prefilled) and Venmo Step 2; email is the alternative. Automatic delivery still needs `VITE_ORDER_ENDPOINT`. Recommended: Formspree free form (5 min, Brenda's account) → add the secret → every order also lands in valeobodycare@gmail.com with no customer action. Alternative with existing infra: a tiny Vercel function relaying to GHL SMS/email (the Vyzible GHL token exists) — needs Brenda's yes (new deploy + outbound messages). | `src/lib/orders.ts` `submitOrder`, `.github/workflows/deploy.yml` secret | Brenda (5 min) |
| 14 | AI jar label read "Calming Botanical Body Butter · Pineapple Coconut Vanilla" on every world | RESOLVED 2026-09-11 late: label edited to lotus + VALEO + est. 2025 + BODY BUTTER (Gemini label edit of the original render, chroma-keyed); used in hero, passage, product tile, single-jar photo. Scrub jar renders still carry their small original label text (illegible at tile size). Real photography is still the gate. | `public/products/jar-body-butter*.webp`, `p-body-butter.webp` | Shauna's shoot |
| 15 | Safari never advanced the passage videos (seeks don't render until a clip has played) | RESOLVED: clips are primed (blob → preload auto → play/pause) before scrubbing; verified in Playwright WebKit with an iPhone UA. Not verified on a physical iPhone (no full Xcode on this Mac for the simulator). | `src/lib/passage.ts` | Brenda: one look on a real iPhone |
